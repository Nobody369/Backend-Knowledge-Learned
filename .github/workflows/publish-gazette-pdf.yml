name: Publish Backend Odyssey Gazette PDF

on:
  workflow_dispatch:
  schedule:
    - cron: "0 14 */3 * *"

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      ODYSSEY_TO_EMAIL: ${{ secrets.ODYSSEY_TO_EMAIL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Build latest issue PDF
        run: npm run build:pdf

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-odyssey-gazette-pdf
          path: dist/pdf/*.pdf

      - name: Email latest PDF
        if: ${{ env.GMAIL_USER != '' && env.GMAIL_APP_PASSWORD != '' && env.ODYSSEY_TO_EMAIL != '' }}
        run: npm run send:latest
